{
  "name": "resulthub",
  "private": true,
  "scripts": {
    "dev": "concurrently -n fe,be -c cyan,green \"npm run dev --prefix frontend\" \"python backend/manage.py runserver 0.0.0.0:8000\"",
    "build": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "backend": "python backend/manage.py runserver 0.0.0.0:8000"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
